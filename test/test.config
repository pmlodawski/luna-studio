name = "pacman"

version = "1.1.0-ajg"

source = [("x86", "uri/to/package")]

install = [
    "unpack",
    "configure",
    "build"
]

uninstall = [
    "rm $(HOME)/.pacman"
]

dependancies = [("stdlib.c", "0-hello", "14.3.2-ultra-stable")]