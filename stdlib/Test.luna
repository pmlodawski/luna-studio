% + OrphanNames

foreign haskell def print s:
    polyJoin . liftF1 (Value . fmap Safe . print) $ s

foreign haskell def Int.+ a:
    liftF2 (+) self a

def foo (a,b):
    a + 1

def a + b:
    a.+ b

def main:
    print ([] :: [Int])
    print (foo (5,"b"))
