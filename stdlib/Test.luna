foreign haskell def print s="":
    polyJoin . liftF1 (Value . fmap Safe . print) $ s

class Vector a:
    x,y,z :: a

    def asTuple: (self.x, self.y, self.z)

def main:
    v = Vector y=1 2 3
    a = v.asTuple
    print a