description = "Awesome plugin"

source = "http://uri.to/package.tar"

install = [
    "unpack",
    "configure",
    "build"
]

uninstall = [
    "rm $(HOME)/.pacman",
    "rm $(HOME)/.pacman"
]

dependencies = [
    "stdlib < 10",
    "stdlib.h < 0",
    "stdio.h < 1.2",
    "blo"
]
