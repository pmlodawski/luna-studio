precision highp float;

varying vec2 luv;
uniform sampler2D texture;
uniform int objectMap;

void main(void) {
    gl_FragColor = texture2D(texture, luv);
    gl_FragColor.a *= float(1 - objectMap);
}
