export class Breadcrumbs
    constructor: (values) ->
        @set values

    set: ({key: @key}) =>
        @updateView()

    updateView: =>

    render: =>

    attach: (scene) =>

    detach: (scene) =>
