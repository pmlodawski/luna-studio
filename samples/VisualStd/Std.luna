def Int.+ a:
    ```liftf2 (+) #{self} #{a}```

def Int.* a:
    ```liftf2 (*) #{self} #{a}```

def Int./ a:
    ```liftf2 (-) #{self} #{a}```

###### Float ##################################

def Float.+ a:
    ```liftf2 (+) #{self} #{a}```

def Float./ a:
    ```liftf2 (/) #{self} #{a}```

def Float.* a:
    ```liftf2 (*) #{self} #{a}```

def Float.- a:
    ```liftf2 (-) #{self} #{a}```
