
###### Float ##################################

class All

def Float.+ self a:
    ```liftf2 (+) #{self} #{a}```

def Float./ self a:
    ```liftf2 (/) #{self} #{a}```

def Float.* self a:
    ```liftf2 (*) #{self} #{a}```

def Float.- self a:
    ```liftf2 (-) #{self} #{a}```

def Int.+ self a:
    ```liftf2 (+) #{self} #{a}```

def Int.* self a:
    ```liftf2 (*) #{self} #{a}```

def Int./ self a:
    ```liftf2 (-) #{self} #{a}```

def print self msg:
    ```print' #{msg}```