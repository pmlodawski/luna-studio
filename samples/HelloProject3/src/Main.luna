def List.each self callback:
    ```let {mymap = liftf2 map}```
    ```mymap (val $ call1 #{callback}) #{self}```

class Console:
    def print self msg:
        ```print' #{msg}```

class Vector a:
    Vector: x,y,z :: a

def main self:
    c = Console()
    p = c.print
    p 1
    p "ala"